arch: s390x
services:
    - docker
script: docker login quay.io -u $ROBOT_USER -p $ROBOTTOKEN && cd 1.15/alpine3.12/ && docker build . --tag quay.io/ibmz/golang:1.15 && docker push quay.io/ibmz/golang:1.15
